<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.17.0" xml:lang="en-US">
  <compounddef id="md_docs_2gaze__math__physical__model" kind="page">
    <compoundname>md_docs_2gaze__math__physical__model</compoundname>
    <title>Gaze Tracking Physical Geometry &amp; Projection Model</title>
    <briefdescription>
    </briefdescription>
    <detaileddescription>
<para><anchor id="md_docs_2gaze__math__physical__model_1autotoc_md12"/></para>
<para>This document explains the physical geometric models and mathematical formulas used by the <bold>godot-gaze</bold> projection engine to map a 3D gaze vector in camera space to 2D screen pixels.</para>
<para><hruler/>
</para>
<sect1 id="md_docs_2gaze__math__physical__model_1autotoc_md14">
<title>1. Coordinate Systems</title><sect2 id="md_docs_2gaze__math__physical__model_1autotoc_md15">
<title>OpenCV Camera Space</title><para><itemizedlist>
<listitem><para><bold>Origin ($C_{\text{cv}}$)</bold>: Optical center of the camera lens.</para>
</listitem><listitem><para><bold>$X_{\text{cv}}$-axis</bold>: Horizontal, pointing right from the camera&apos;s perspective (corresponding to the user&apos;s left).</para>
</listitem><listitem><para><bold>$Y_{\text{cv}}$-axis</bold>: Vertical, pointing down.</para>
</listitem><listitem><para><bold>$Z_{\text{cv}}$-axis</bold>: Optical axis, pointing forward into the camera&apos;s view cone (towards the user).</para>
</listitem></itemizedlist>
</para>
</sect2>
<sect2 id="md_docs_2gaze__math__physical__model_1autotoc_md16">
<title>OpenCV Face Space</title><para>This is the space of the 3D facial model used by YuNet&apos;s <computeroutput>solvePnP</computeroutput> solver (eyes, nose, mouth corners):<itemizedlist>
<listitem><para><bold>Origin</bold>: Midpoint of the eyes at depth 0.</para>
</listitem><listitem><para><bold>$X_{\text{face_cv}}$-axis</bold>: Horizontal, pointing to the face&apos;s own left (image right).</para>
</listitem><listitem><para><bold>$Y_{\text{face_cv}}$-axis</bold>: Vertical, pointing down.</para>
</listitem><listitem><para><bold>$Z_{\text{face_cv}}$-axis</bold>: Perpendicular to the face plane, pointing to the back of the head. (The nose points along $-Z_{\text{face_cv}}$).</para>
</listitem></itemizedlist>
</para>
</sect2>
<sect2 id="md_docs_2gaze__math__physical__model_1autotoc_md17">
<title>GodotGaze Camera Space (Standard Camera Space)</title><para>This space is aligned with standard right-handed graphics conventions:<itemizedlist>
<listitem><para><bold>Origin ($C_{\text{cam}}$)</bold>: Optical center of the camera lens.</para>
</listitem><listitem><para><bold>$X_{\text{cam}}$-axis</bold>: Horizontal, pointing right from the camera&apos;s perspective (left of display/user&apos;s left).</para>
</listitem><listitem><para><bold>$Y_{\text{cam}}$-axis</bold>: Vertical, pointing up.</para>
</listitem><listitem><para><bold>$Z_{\text{cam}}$-axis</bold>: Perpendicular to the display, pointing out the back of the camera <bold>away</bold> from the camera&apos;s view cone. Thus, $-Z_{\text{cam}}$ points towards the user in front of the display, meaning the user is located at negative Z ($z_{\text{cam}} &lt; 0$).</para>
</listitem><listitem><para><bold>Mapping from OpenCV Camera Space</bold>: To correct the vertical direction (Y points down in OpenCV, up in GodotGaze) and the forward/backward direction (Z points forward/into-view-cone in OpenCV, backward/away-from-view-cone in GodotGaze), we apply a **$180^\circ$ pitch rotation around the X-axis**: $$M = R_X(180^\circ) = \begin{pmatrix} 1 &amp; 0 &amp; 0 \ 0 &amp; -1 &amp; 0 \ 0 &amp; 0 &amp; -1 \end{pmatrix}$$ This maps the coordinates as: $$X_{\text{cam}} = X_{\text{cv}}$$ $$Y_{\text{cam}} = -Y_{\text{cv}}$$ $$Z_{\text{cam}} = -Z_{\text{cv}}$$</para>
</listitem></itemizedlist>
</para>
</sect2>
<sect2 id="md_docs_2gaze__math__physical__model_1autotoc_md18">
<title>GodotGaze Face Space (Face Space)</title><para>This is the standard local coordinate space of the user&apos;s head:<itemizedlist>
<listitem><para><bold>Origin</bold>: Midpoint of the eyes.</para>
</listitem><listitem><para><bold>$X_{\text{local}}$-axis</bold>: Horizontal, pointing to the user&apos;s right ear.</para>
</listitem><listitem><para><bold>$Y_{\text{local}}$-axis</bold>: Vertical, pointing up the face.</para>
</listitem><listitem><para><bold>$Z_{\text{local}}$-axis</bold>: Perpendicular to the face plane, pointing to the back of the head. Thus, the nose points along $-Z_{\text{local}}$ (forward).</para>
</listitem><listitem><para><bold>Mapping to OpenCV Face Space</bold>: To align with OpenCV&apos;s face coordinate system (where Y points down the face and X points to the face&apos;s own left), we apply a **$180^\circ$ roll rotation around the Z-axis**: $$R_Z(180^\circ) = \begin{pmatrix} -1 &amp; 0 &amp; 0 \ 0 &amp; -1 &amp; 0 \ 0 &amp; 0 &amp; 1 \end{pmatrix}$$ This flips both the local X and Y axes while keeping the Z-axis (pointing to the back of the head) unchanged: $$X_{\text{face_cv}} = -X_{\text{local}}$$ $$Y_{\text{face_cv}} = -Y_{\text{local}}$$ $$Z_{\text{face_cv}} = Z_{\text{local}}$$ Note that both spaces share the same $+Z$ direction (pointing to the back of the head, away from the camera). However, $+X$ points to the face&apos;s own left in OpenCV face space and to the user&apos;s right in GodotGaze Face Space, and $+Y$ points down in OpenCV face space and up in GodotGaze Face Space. Thus, the mapping is a pure $180^\circ$ rotation around the Z-axis ($R_Z(180^\circ)$), which flips both X and Y while preserving Z.</para>
</listitem></itemizedlist>
</para>
</sect2>
<sect2 id="md_docs_2gaze__math__physical__model_1autotoc_md19">
<title>1.5. Transformation between Spaces</title><para>Any local point $P_{\text{local}}$ in GodotGaze Face Space is mapped to GodotGaze Camera Space $P_{\text{cam}}$ via: $$P_{\text{cam}} = R_{\text{cam}} \cdot P_{\text{local}} + t_{\text{cam}}$$ where: $$R_{\text{cam}} = M \cdot R_{\text{cv}} \cdot M \cdot R_Y(180^\circ)$$ $$t_{\text{cam}} = M \cdot t_{\text{cv}}$$ Here, $R_{\text{cv}}$ and $t_{\text{cv}}$ are the rotation and translation returned by OpenCV&apos;s <computeroutput>solvePnP</computeroutput> solver.</para>
<para>This can be expressed as a chain of 3D transforms: $$T_{\text{ggaze_face_to_ggaze_cam}} = T_{\text{cv_cam_to_ggaze_cam}} \cdot T_{\text{cv_face_to_cv_cam}} \cdot T_{\text{ggaze_face_to_cv_face}}$$ where:<itemizedlist>
<listitem><para>$T_{\text{cv_cam_to_ggaze_cam}} = \text{Transform}(R_X(180^\circ), \vec{0})$ is the $180^\circ$ pitch rotation mapping OpenCV Camera Space to GodotGaze Camera Space.</para>
</listitem><listitem><para>$T_{\text{cv_face_to_cv_cam}} = \text{Transform}(R_{\text{cv}}, t_{\text{cv}})$ is the OpenCV Face-to-Camera transform.</para>
</listitem><listitem><para>$T_{\text{ggaze_face_to_cv_face}} = \text{Transform}(R_Z(180^\circ), \vec{0})$ is the $180^\circ$ roll rotation mapping GodotGaze Face Space to OpenCV Face Space. *(Note that the net effect $R_X(180^\circ) \cdot R_Z(180^\circ) = R_Y(180^\circ)$, which is a $180^\circ$ yaw rotation around the vertical axis).*</para>
</listitem></itemizedlist>
</para>
</sect2>
<sect2 id="md_docs_2gaze__math__physical__model_1autotoc_md20">
<title>Physical Display Space (Monitor Local Space)</title><para>This centered millimeter coordinate system is standard for desktop gaze geometry modeling (e.g. eye-tracking and OpenCV solvers) to define symmetric screen planes:<itemizedlist>
<listitem><para><bold>Origin ($S$)</bold>: Center of the physical display/monitor screen.</para>
</listitem><listitem><para><bold>X-axis</bold>: Horizontal, pointing right (in mm).</para>
</listitem><listitem><para><bold>Y-axis</bold>: Vertical, pointing down (in mm).</para>
</listitem><listitem><para><bold>Z-axis</bold>: Perpendicular to the screen plane, pointing toward the user (in mm).</para>
</listitem><listitem><para>The flat display plane is defined by the equation $z_{screen} = 0$.</para>
</listitem></itemizedlist>
</para>
<para><hruler/>
</para>
</sect2>
</sect1>
<sect1 id="md_docs_2gaze__math__physical__model_1autotoc_md22">
<title>2. Camera-to-Display Transformation</title><para>Let the camera&apos;s physical position in Physical Display Space be configured as:<itemizedlist>
<listitem><para><bold>Camera Offset ($O_{cam}$)</bold>: Vector $(x_{off}, y_{off}, z_{off})$ in mm.</para>
</listitem><listitem><para>**Camera Tilt ($\theta$)**: Downward tilt angle in degrees about the camera&apos;s local X-axis.</para>
</listitem></itemizedlist>
</para>
<para>The rotation matrix $R$ rotating vectors from Camera Space to Physical Display Space (for tilt angle $\theta$ in radians) is: $$R = R_x(\theta) = \begin{pmatrix} 1 &amp; 0 &amp; 0 \ 0 &amp; \cos\theta &amp; -\sin\theta \ 0 &amp; \sin\theta &amp; \cos\theta \end{pmatrix}$$</para>
<para>For any point $P_{cam}$ in Camera Space, its position in Physical Display Space is: $$P_{screen} = R \cdot P_{cam} + O_{cam}$$</para>
<para>Substituting components: $$x_s = x_{cam} + x_{off}$$ $$y_s = y_{cam} \cos\theta - z_{cam} \sin\theta + y_{off}$$ $$z_s = y_{cam} \sin\theta + z_{cam} \cos\theta + z_{off}$$</para>
<para><hruler/>
</para>
</sect1>
<sect1 id="md_docs_2gaze__math__physical__model_1autotoc_md24">
<title>3. 3D Ray-Plane Intersection</title><para>A gaze ray starting at origin $P_{0_cam} = (x_0, y_0, z_0)$ with normalized direction vector $V_{cam} = (v_x, v_y, v_z)$ in Camera Space is parameterized by $t$: $$P_{cam}(t) = P_{0_cam} + t \cdot V_{cam}$$</para>
<para>To find where it intersects the screen plane, we project the ray into Physical Display Space and solve for $z_s(t) = 0$: $$z_s(t) = (y_0 + t v_y) \sin\theta + (z_0 + t v_z) \cos\theta + z_{off} = 0$$</para>
<para>Solving for $t$: $$t = - \frac{y_0 \sin\theta + z_0 \cos\theta + z_{off}}{v_y \sin\theta + v_z \cos\theta}$$</para>
<para>If $t &lt; 0$, the gaze ray points away from the screen (no intersection). Otherwise, we compute the camera-space intersection point: $$P_{int_cam} = P_{0_cam} + t \cdot V_{cam}$$</para>
<para>And transform it to the display physical coordinates $(x_s, y_s)$ in mm: $$x_s = P_{int_cam}.x + x_{off}$$ $$y_s = P_{int_cam}.y \cos\theta - P_{int_cam}.z \sin\theta + y_{off}$$</para>
<para><hruler/>
</para>
</sect1>
<sect1 id="md_docs_2gaze__math__physical__model_1autotoc_md26">
<title>4. Monitor-to-Viewport Pixel Mapping</title><para>Let:<itemizedlist>
<listitem><para>Screen resolution in pixels be $(W_{px}, H_{px})$ (e.g., $1920 \times 1080$).</para>
</listitem><listitem><para>Screen physical size in mm be $(W_{mm}, H_{mm})$ (e.g., $527 \times 296$).</para>
</listitem></itemizedlist>
</para>
<para>We map $(x_s, y_s)$ in mm relative to the screen center to monitor-absolute pixels $(x_{px}, y_{px})$, where the top-left of the monitor is $(0, 0)$: $$x_{px} = \frac{W_{px}}{2} + x_s \cdot \frac{W_{px}}{W_{mm}}$$ $$y_{px} = \frac{H_{px}}{2} + y_s \cdot \frac{H_{px}}{H_{mm}}$$</para>
<sect2 id="md_docs_2gaze__math__physical__model_1autotoc_md27">
<title>Viewport Localization</title><para>In a windowed Godot game, the final viewport/window-local coordinate is computed by subtracting the window offset: $$x_{viewport} = x_{px} - \text{window_pos.x}$$ $$y_{viewport} = y_{px} - \text{window_pos.y}$$</para>
<para><hruler/>
</para>
</sect2>
</sect1>
<sect1 id="md_docs_2gaze__math__physical__model_1autotoc_md29">
<title>5. Calibration &amp; Error Correction</title><para>Individual eye shape, eyeball depth, and camera mount errors cause systematic estimation deviations (typically $O(2^\circ - 5^\circ)$). We correct this using two models:</para>
<sect2 id="md_docs_2gaze__math__physical__model_1autotoc_md30">
<title>3D Spherical Angular Calibration</title><para>We apply pitch/yaw angle biases to the raw gaze vector $V = (v_x, v_y, v_z)$ before intersection calculation:<orderedlist>
<listitem><para>Extract spherical angles: $$\phi_{yaw} = \operatorname{atan2}(v_x, v_z)$$ $$\psi_{pitch} = \operatorname{asin}(v_y)$$</para>
</listitem><listitem><para>Apply calibration biases: $$\phi_{calib} = \phi_{yaw} + \text{bias_yaw}$$ $$\psi_{calib} = \psi_{pitch} + \text{bias_pitch}$$</para>
</listitem><listitem><para>Re-project to the calibrated unit vector: $$V_{calib} = (\sin\phi_{calib} \cos\psi_{calib},\; \sin\psi_{calib},\; \cos\phi_{calib} \cos\psi_{calib})$$</para>
</listitem></orderedlist>
</para>
<para>During calibration trigger (staring at a target screen pixel $P_{target}$):<orderedlist>
<listitem><para>Transform $P_{target}$ back to Camera Space ($P_{cam_target}$) by reversing the rotation and translation: $$x_{cam_target} = x_{s_target} - x_{off}$$ $$y_{cam_target} = (y_{s_target} - y_{off}) \cos\theta - z_{off} \sin\theta$$ $$z_{cam_target} = -(y_{s_target} - y_{off}) \sin\theta - z_{off} \cos\theta$$</para>
</listitem><listitem><para>The required gaze vector is: $$V_{req} = (P_{cam_target} - P_{0_cam}).normalized()$$</para>
</listitem><listitem><para>Compute the angular differences to store as $\text{bias_pitch}$ and $\text{bias_yaw}$: $$\text{bias_yaw} = \phi_{req} - \phi_{yaw}$$ $$\text{bias_pitch} = \psi_{req} - \psi_{pitch}$$</para>
</listitem></orderedlist>
</para>
</sect2>
<sect2 id="md_docs_2gaze__math__physical__model_1autotoc_md31">
<title>2D Pixel-Space Calibration</title><para>Simple translational delta applied after the pixel mapping: $$x_{px_final} = x_{px_projected} + \text{bias_pixel_x}$$ $$y_{px_final} = y_{px_projected} + \text{bias_pixel_y}$$</para>
<para>During calibration trigger, the biases are directly calculated as: $$\text{bias_pixel_x} = x_{target} - x_{projected}$$ $$\text{bias_pixel_y} = y_{target} - y_{projected}$$</para>
<para><hruler/>
</para>
</sect2>
</sect1>
<sect1 id="md_docs_2gaze__math__physical__model_1autotoc_md33">
<title>6. Real-Time Depth Triangulation (Z Engine)</title><para>Using a pinhole camera model, the distance $Z$ from the camera sensor is calculated from:<itemizedlist>
<listitem><para><bold>Interpupillary Distance (IPD)</bold>: $63.0$ mm (constant average adult).</para>
</listitem><listitem><para><bold>Focal Length in Pixels ($f_{px}$)</bold>: Screen width or height scale multiplier.</para>
</listitem><listitem><para><bold>Pixel Distance ($d_{px}$)</bold>: Detected distance between eye centers in the 2D frame.</para>
</listitem></itemizedlist>
</para>
<para>$$Z_{mm} = \frac{IPD_{mm} \cdot f_{px}}{d_{px}}$$ $$Z_{cm} = \frac{Z_{mm}}{10.0}$$</para>
<para><hruler/>
</para>
</sect1>
<sect1 id="md_docs_2gaze__math__physical__model_1autotoc_md35">
<title>7. OpenVINO Gaze Estimation Model (ADAS-0002) Conventions</title><para>The OpenModelZoo gaze estimation network (<computeroutput>gaze-estimation-adas-0002</computeroutput>) operates with distinct input/output coordinate space and sign conventions which are reconciled in the wrapper layer:</para>
<sect2 id="md_docs_2gaze__math__physical__model_1autotoc_md36">
<title>Input Feature Preprocessing</title><para><itemizedlist>
<listitem><para><bold>Eye Crop Inputs</bold>: The model defines its inputs from the camera/viewer&apos;s perspective.<itemizedlist>
<listitem><para><computeroutput>"left_eye_image"</computeroutput> receives the crop of the eye appearing on the <bold>left side of the image frame</bold> (which is the subject&apos;s anatomical <bold>right eye</bold>).</para>
</listitem><listitem><para><computeroutput>"right_eye_image"</computeroutput> receives the crop of the eye appearing on the <bold>right side of the image frame</bold> (the subject&apos;s anatomical <bold>left eye</bold>).</para>
</listitem><listitem><para><emphasis>Effect</emphasis>: The eye crops are swapped relative to anatomical labeling when passed to the model.</para>
</listitem></itemizedlist>
</para>
</listitem><listitem><para><bold>Head Pose Sign Alignment</bold>: The model expects input head pose angles in degrees with positive-left (yaw), positive-down (pitch), and positive-clockwise (roll) orientations:<itemizedlist>
<listitem><para><bold>Yaw</bold>: Negated (<computeroutput>-crops.head_pose_rotation.y</computeroutput>), mapping negative SolvePnP yaw to positive model yaw.</para>
</listitem><listitem><para><bold>Pitch</bold>: Direct (<computeroutput>crops.head_pose_rotation.x</computeroutput>).</para>
</listitem><listitem><para><bold>Roll</bold>: Negated (<computeroutput>-crops.head_pose_rotation.z</computeroutput>), aligning the roll coordinate signs.</para>
</listitem></itemizedlist>
</para>
</listitem></itemizedlist>
</para>
</sect2>
<sect2 id="md_docs_2gaze__math__physical__model_1autotoc_md37">
<title>Output Vector Mapping</title><para>The 3D direction vector output by the model (<computeroutput>raw_gaze_dir</computeroutput>) is mapped to GodotGaze Camera Space:<itemizedlist>
<listitem><para><bold>X Component</bold>: Direct (<computeroutput>raw_gaze_dir.x</computeroutput>), as $+X$ points right (camera&apos;s left / user&apos;s right) in both spaces.</para>
</listitem><listitem><para><bold>Y Component</bold>: Direct (<computeroutput>raw_gaze_dir.y</computeroutput>), as $+Y$ points UP in both spaces.</para>
</listitem><listitem><para><bold>Z Component</bold>: Negated (<computeroutput>-raw_gaze_dir.z</computeroutput>), reversing the optical direction so the unit vector points forward towards the screen plane ($Z_{\text{cam}} = 0$, $v_z &gt; 0$) rather than backward into the camera ($v_z &lt; 0$).</para>
</listitem><listitem><para><emphasis>Note</emphasis>: The model outputs its gaze vector in its own left-handed space (+X right, +Y up, +Z forward towards the user). To transform this left-handed vector to the right-handed GodotGaze Camera Space (+X right, +Y up, +Z backward), we preserve X and Y and negate Z. This reflection transforms the coordinate systems correctly.</para>
</listitem></itemizedlist>
</para>
<para><hruler/>
</para>
</sect2>
</sect1>
<sect1 id="md_docs_2gaze__math__physical__model_1autotoc_md39">
<title>8. Pipeline Configurations (GazePipelineConfig)</title><para>The pipeline parameters can be customized dynamically using the <computeroutput>GazePipelineConfig</computeroutput> resource:<itemizedlist>
<listitem><para><computeroutput>pitch_t_gain</computeroutput> (default: <computeroutput>0.5</computeroutput>): Gain multiplier applied to vertical translation ($t_y$) to compensate for the coplanar landmark PnP vertical pitch ambiguity.</para>
</listitem><listitem><para><computeroutput>yaw_t_gain</computeroutput> (default: <computeroutput>0.0</computeroutput>): Gain multiplier applied to horizontal translation ($t_x$) to compensate for the PnP yaw ambiguity (disabled by default as yaw is highly stable).</para>
</listitem><listitem><para><computeroutput>nose_z</computeroutput> (default: <computeroutput>-45.0</computeroutput> mm): Physical nose tip depth relative to the eye plane. Affects the vertical pitch sensitivity in the PnP solver.</para>
</listitem><listitem><para><computeroutput>ipd_mm</computeroutput> (default: <computeroutput>63.0</computeroutput> mm): Interpupillary distance in millimeters, used for real-time depth triangulation. </para>
</listitem></itemizedlist>
</para>
</sect1>
    </detaileddescription>
    <location file="docs/gaze_math_physical_model.md"/>
  </compounddef>
</doxygen>
