<?xml version="1.0" encoding="UTF-8" ?>
<class name="GazeTracker" inherits="Node">
	<brief_description>GazeTracker.</brief_description>
	<description>Declarative Scene Node for managing GazeServer tracking lifecycle.

Attaching this node to a scene tree increments GazeServer's reference counter on tree entry and decrements it on tree exit. Multiple GazeTracker nodes can safely coexist in the scene hierarchy; tracking is active as long as at least one tracker is in the active scene tree.

It also provides the static helper GazeTracker.track_node(node) for programmatic lifecycle attachment from custom GDScript scripts.</description>
	<tutorials></tutorials>
	<methods>
		<method name="set_enabled">
			<return type="void"></return>
			<param index="0" name="p_enabled" type="bool"></param>
			<description>Toggles tracker activation.

If disabled while inside the scene tree, releases its tracking reference count. If re-enabled while inside the scene tree, acquires a tracking reference count.</description>
		</method>
		<method name="is_enabled">
			<return type="bool"></return>
			<description></description>
		</method>
		<method name="is_tracking_active">
			<return type="bool"></return>
			<description>Returns true if this specific GazeTracker instance is currently holding an active reference count in GazeServer.</description>
		</method>
		<method name="track_node">
			<return type="GazeTracker *"></return>
			<param index="0" name="p_node" type="Node *"></param>
			<description>Programmatic helper to attach an internal GazeTracker child to an arbitrary node.

In GDScript: func _init() -&gt; void: GazeTracker.track_node(self)

Uses Node::INTERNAL_MODE_FRONT so the child node is invisible to get_children(), not shown in the editor scene tree, and excluded from scene serialization.</description>
		</method>
	</methods>
	<members></members>
	<signals></signals>
</class>