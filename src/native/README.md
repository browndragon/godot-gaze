/**
 * @dir src/native
 * @brief Platform-native dependencies, ML pipelines, and model definitions.
 *
 * This directory contains the platform-agnostic native implementations of ONNX Runtime
 * model pipeline wrappers and platform capture drivers.
 */

# Native Layer

The Native layer hosts the ONNX Runtime machine learning pipelines for face detection (YuNet) and gaze estimation.
